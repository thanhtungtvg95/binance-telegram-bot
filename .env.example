# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
TELEGRAM_CHAT_ID=your_telegram_chat_id_here

# Optional: Binance API keys if needed (for public endpoints it is not strictly required, but good practice if hitting limits)
# BINANCE_API_KEY=your_binance_api_key
# BINANCE_SECRET=your_binance_secret

# Optional: Scan Interval in minutes (if running automatically)
SCAN_INTERVAL=240
